<?xml version="1.0" encoding="utf-8"?>
<!-- 1. Đổi layout gốc sang HÀNG NGANG (horizontal) -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp"
    android:gravity="center_vertical">

    <!-- 2. Giữ LinearLayout (DỌC) của bạn để bọc 2 textview -->
    <!-- Thêm layout_weight="1" để nó chiếm hết không gian bên trái -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_notification_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"

            android:textAppearance="@style/TextAppearance.Material3.TitleMedium"
            android:textColor="@color/md_theme_surfaceTint"
            tools:text="Nhắc nhở Lịch hẹn" />

        <TextView
            android:id="@+id/tv_notification_body"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            tools:text="Lịch hẹn của bạn với B.S. Olivia Turner là vào 10:00 sáng mai." />

    </LinearLayout>

    <ImageButton
        android:id="@+id/ib_delete_notification"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="16dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Xóa thông báo"
        android:src="@drawable/ic_delete"/>

</LinearLayout>